package authority

// Types are declared in resolver.go; this file ensures package-level cohesion.
// Additional authority types (transition events, workspace targets) live in
// the parent runtime package and are not duplicated here to preserve the
// single-authority invariant.
